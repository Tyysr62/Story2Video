# Web App Environment Variables

# API Base URL for REST API calls
VITE_API_BASE_URL=http://localhost:8080

# WebSocket URL for real-time progress updates
VITE_WS_URL=ws://localhost:8080/stream

# 是否使用 Mock 数据（开发模式）
# 设置为 "true" 启用 mock 客户端，返回预定义的测试数据
VITE_USE_MOCK=false
